#include<stdio.h>
#include<stdlib.h>
#include "header.h"


 //////////////////////////
//////////////////77
//sensor.txt dosyasından gelen verileri düzgün olarak parçalayarak okumayı yapabildim
//ama bunu grapa değer olarak geçirme kısmını ypamadığım için sorunun geri kalan kısmını yapamadım.
//9.soruya kadar işelm yapılmıştır.
int main(){
 int V = 5;
    struct Graph* graph = createGraph(V);
    //addEdge(graph, 0, 1,3);
    //addEdge(graph, 0, 4,4);
   
 
    // print the adjacency list representation of the above graph
    //printGraph(graph);
 
    return 0;

 	Sensor();
 
    return 0;
}
